<odoo>
    <record id="view_grade_list" model="ir.ui.view">
        <field name="name">grade</field>
        <field name="model">grade.scale</field>
        <field name="arch" type="xml">
            <list string="Grade">
                <field name="name" />
            </list>
        </field>
    </record>

    <record id="view_grade_form" model="ir.ui.view">
        <field name="name">grade.form</field>
        <field name="model">grade</field>
        <field name="arch" type="xml">
            <form string="Grade Details">
                <sheet>
                    <!-- <div class="oe_button_box" name="button_box">
                            <button name="get_grade_teachers" icon="fa-users" type="object" class="oe_stat_button">
                                    <div class="o_stat_info">
                                        <span class="o_stat_value">
                                            <field name="teacher_count"/>
                                        </span>
                                        <span class="o_stat_text">
                                            Teachers
                                        </span>
                                    </div>
                            </button>
                        </div> -->

                    <div class="oe_title">
                        <h1>
                            <label for="name" string="Grade" />
                            <field name="name" placeholder="Ex. 10th" />
                        </h1>
                    </div>
                    <group name="grade_details1">
                        <group>
                            <!-- <field name="scale_id" options="{'no_create_edit': True, 'no_create': True, 'no_edit': True}"/> -->
                        </group>
                        <group>
                            <field
                                name="company_id"
                                readonly="is_single_company"
                                options="{'no_create_edit': True, 'no_create': True, 'no_edit': True}"
                                groups="base.group_multi_company"
                            />
                        </group>
                    </group>
                    <notebook>
                        <!-- <page name="subjects" string="Subjects">
                                <field name="subject_ids">
                                    <list editable="bottom">
                                        <field name="name"/>
                                        <field name="subject_code"/>
                                        <field name="credit_value"/>
                                        <field name="optional_choice" widget="checkbox"/>
                                        <field name="grade_scale_id" required="1" readonly="optional_choice == False" force_save="1" context="{'default_scale_id': scale_id}" domain="[('gpa_calculation', '=', False)]" options="{'no_create': True, 'no_edit': True}"/>
                                    </list>
                                </field>
                            </page> -->
                        <!-- <page name="sections" string="Sections">
                                <field name="section_ids">
                                    <list editable="bottom">
                                        <field name="name"/>
                                        <field name="company_id" readonly="is_single_company" groups="base.group_multi_company"/>
                                        <field name="create_date"/>
                                    </list>
                                </field>
                            </page> -->
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_grade" model="ir.actions.act_window">
        <field name="name">grade</field>
        <field name="res_model">grade</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_grade_scale_search" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Grade
            </p>
        </field>
    </record>
</odoo>
